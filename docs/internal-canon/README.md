# Internal Canon Docs

This folder stores working canon documents used for internal product/engineering alignment.

Rules:
- Keep canonical docs here when they must be versioned.
- Do not place these files at repo root.
- Root-level copies are intentionally ignored via `.gitignore`.
